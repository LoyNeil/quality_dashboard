{
  "name": "express-backend",
  "version": "1.0.0",
  "description": "Express.js backend for my project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",    // this should start your backend server
    "dev": "nodemon server.js",   // for local development
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Loy Neil",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0"
  }
}
